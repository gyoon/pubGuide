{
  "name": "kdb",
  "version": "0.0.1",
  "scripts": {
    "start": "gulp build && http-server -p 3002 -c-1",
    "dev": "gulp dev",
    "init": "npm install && bower install",
    "build": "gulp build"
  },
  "dependencies": {
    "browser-sync": "^2.11.2",
    "gulp": "^3.9.1",
    "gulp-clean": "^0.3.2",
    "gulp-concat": "^2.6.0",
    "gulp-filter": "^4.0.0",
    "gulp-flatten": "^0.2.0",
    "gulp-sass": "^2.2.0",
    "http-server": "^0.9.0",
    "node-sass": "^3.13.0"
  },
  "devDependencies": {}
}
